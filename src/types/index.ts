export type Organization = {
  id: number;
  pid: string;
  name: string;
};

export type Instructor = {
  id: number;
  pid: string;
  name: string;
  companyId: string;
};

export type Subject = {
  id: number;
  pid: string;
  name: string;
  companyId: string;
};
